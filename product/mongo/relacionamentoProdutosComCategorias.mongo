db.products.find().forEach((produto) => {
  info = db.categories
    .find({ nome: produto.CATEGORIA }, { _id: 1, nome: 1, status: 1 })
    .toArray();

  db.products.updateMany(
    { CATEGORIA: info[0].nome },
    { $set: { CATEGORIA: info[0] } }
  );
});
